import { ModelDef } from "@/types/model"
export default function Options({modal} : {modal: ModelDef}){
    return(
        <div>
            <p>
                On the Options Page
            </p>
            
        </div>
    )
}